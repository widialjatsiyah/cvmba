@inject('request', 'Illuminate\Http\Request')
<!-- Main Header -->
  <header class="main-header bg-primary">
    <a href="{{route('home')}}" style="text-align: center;">
      
      <!-- <span >{{ Session::get('business.name') }} <i class="fa fa-circle text-success" id="online_indicator"></i></span>  -->
      <img src="{{asset('images/logo-panjang.png')}}" alt="Logo" width="150px">
       
    </a>

   
  </header>